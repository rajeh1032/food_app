# Build configuration for code generation
targets:
  $default:
    builders:
      injectable_generator:injectable_builder:
        enabled: true
        options:
          auto_register: true
          # Generate for files in lib folder
          generate_for:
            - lib/**
